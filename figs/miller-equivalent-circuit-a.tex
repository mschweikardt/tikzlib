\newcommand{\MillerEquivalentCircuitA}
{
    \newcommand\distA{2.2}
    \newcommand\distB{1.2}
    \newcommand\distC{1.4}
    \newcommand\distD{1.2}
    \newcommand\distE{1.75}
    \newcommand\distF{1.75} 
    \newcommand\distG{1.3} 

    \coordinate (gm1p) at (0,0);

    \draw (gm1p) to [ american controlled current source
                    , name=gmd
                    , l=$g_{\mathrm{m2}}$
                    ] (0,-\distA) coordinate (gm1n);


    \draw (gm1n) to [short,*-o] ++ (-\distB,0) coordinate (in);

    \draw (gm1p-|in) coordinate (ip) to [short,o-] ++ (\distB/3,0); 

    \draw (gm1n) to [short,-]                  ++ (\distC,0)  coordinate (r1n)
                 to [ R
                    , l_=$R_{\mathrm{X}}$
                    ,*-*
                    ] (r1n|-gm1p) coordinate (r1p);

    \draw (r1n) to [short,-*]              ++ (\distD,0)  coordinate (c1n)
                to [ C
                   , l_=$C_{\mathrm{X}}$
                   ]                          (r1p-|c1n)  coordinate (c1p)
                to [short,-*]                 (gm1p);

    \draw (c1p)  to [short,*-o]  ++ (\distE,0)              coordinate (ip2)
                 to [R,l=$R_{\mathrm{c}}$,-]  ++ (\distF,0)
                 to [C,l=$C_{\mathrm{c}}$,-]  ++ (\distF,0) coordinate (gm2p)
                 to [ american controlled current source
                    , l=$g_{\mathrm{m4}}$
                    , -*
                    ] ++ (0,-\distA)                        coordinate (gm2n)
                 to [short,-o]   (gm2n-|ip2)                coordinate (in2)
                 to [short,-*]   (in2-|c1n);

    \draw (gm2n) to [short,-]                  ++ (\distC,0)  coordinate (r2n)
                 to [ R
                    , l_=$R_{\mathrm{Y}}$
                    ,*-*
                    ] (r2n|-gm2p) coordinate (r2p);

    \draw (r2n) to [short,-*]              ++ (\distD,0)  coordinate (c2n)
                to [ C
                   , l_=$C_{\mathrm{Y}}$
                   ]                          (r2p-|c2n)  coordinate (c2p)
                to [short,-*]                 (gm2p);     

    \draw (c2p) to [short,*-o] ++ (\distG,0)            coordinate (op)       
                to [short,-  ] ++ (\distG,0)            coordinate (clp)  
                to [C,l=$C_{\mathrm{L}}$,-]  (clp|-c2n) coordinate (cln)
                to [short,-o ]    (cln-|op)             coordinate (on) 
                to [short,-  ]    (c2n);

    \path [voltarrow] (ip) edge[] node [midway,left,inner sep=2pt] 
      {$V_{\mathrm{I}}$} (in);
    \path [voltarrow] (ip2) edge[] node [midway,left,inner sep=2pt] 
      {$V_{\mathrm{X}}$} (in2);     
    \path [voltarrow] (op) edge[] node [midway,right,inner sep=2pt] 
      {$V_{\mathrm{O}}$} (on); 
}